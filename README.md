# AIRA Solutions — Corporate Clean Vite + React Project

This project contains two parts:

- `/` Landing page (corporate clean)
- `#/slides` Slide-style presentation (hash router)

Commands:

```
npm install
npm run dev
npm run build
```

Deployment:
Upload this ZIP to Vercel (New Project → Upload) or push to GitHub and connect to Vercel.
